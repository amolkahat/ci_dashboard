import React, { Component, useState } from "react";
import axios from 'axios';
import RRToolsTab from "../components/Tabs";

const RRTools = (props) => {
    const [launchpad_bugs, setlaunchpad_bugs] = useState("")

    return <RRToolsTab/>
}

export default RRTools;